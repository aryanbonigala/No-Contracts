#!/usr/bin/env python3
"""Future entrypoint: materialize train/val/test splits in storage (stub)."""

from __future__ import annotations

from kalshi_no_carry.logging_setup import configure_logging

if __name__ == "__main__":
    configure_logging()
    raise SystemExit("build_splits.py is a stub in v0.1 — implement clustering + DB first.")
